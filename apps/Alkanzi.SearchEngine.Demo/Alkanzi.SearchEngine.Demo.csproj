<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Alkanzi.SearchEngine.Demo</RootNamespace>
  </PropertyGroup>

  <!--
    UI-only host. The AngularJS front end calls the Alkanzi.Api backend
    (see appsettings.json → Api:BaseUrl). No data/search references here.
  -->

</Project>
