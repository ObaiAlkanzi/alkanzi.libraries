{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//": "Template only — no password here, this file is committed. The real string, with credentials, comes from user secrets: dotnet user-secrets set \"ConnectionStrings:Postgres\" \"Host=...;Password=...\"",
    "Postgres": "Host=localhost;Port=5432;Database=alkanzi_erp;Username=postgres;Include Error Detail=true"
  }
}
